# 安装

## 环境要求

- Vue 3.5 或更高版本
- Node.js 18 或更高版本

## 使用包管理器

```bash
pnpm add @keyment/components
```

也可以使用 npm：

```bash
npm install @keyment/components
```

::: tip 本地开发
当前文档项目通过 `workspace:*` 使用 Monorepo 中的本地组件包，不需要先发布到 npm。
:::
